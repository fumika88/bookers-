<h1>Editing Book</h1>
<%= form_with model:@book, url:"/book/#{@book.id}", local:true do |f| %>
  <h4>Tittle</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>

  <p><%= f.submit 'Update book' %></p>

  <h4>Show  Back</h4>
<% end %>